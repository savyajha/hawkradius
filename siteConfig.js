module.exports = {
    // Do not include a trailing slash!
    siteUrl: `https://hawkradius.com`,

    // Maximum number of post shown per page
    // With Infinite Scroll this is the chunk of posts fetched when scrolling down
    postsPerPage: 5,

    // This allows an alternative site title for meta data for pages.
    siteTitleMeta: `Hawkradius`,

    // This allows an site description for meta data for pages.
    siteDescriptionMeta: `A blog about history, current affairs, policy and science`,

    // Change to the width and height of your default share image
    shareImageWidth: 1000,
    shareImageHeight: 523,

    // Used for App and Offline manifest e.g. Mobile Home Screen
    shortTitle: `Hawkradius`,
    siteIcon: `favicon.png`,
    backgroundColor: `#e9e9e9`,
    themeColor: `#15171A`,

    gatsbyImages: false,

    // Show more logs for debugging purposes (default: false)
    verbose: false,

    // Severity for verbose mode: (`info`, `warn`, `error`)
    severity: `info`,
}
